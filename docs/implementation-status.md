# Promptarium 実装進捗

**状態:** M11実装・検証完了
**最終更新:** 2026-08-28

このファイルを実装の進捗に合わせて更新する。`[x]` は検証済み完了、`[-]` は作業中、`[ ]` は未着手を表す。

| 状態 | マイルストーン | 現在の内容 |
| --- | --- | --- |
| [x] | M0 要件・方針 | MVP範囲、UI、スキャン、評価、重複方針を記録済み |
| [x] | M1 プロジェクト基盤 | WPFプロジェクトとSQLite依存関係を確認し、`dotnet build src/Promptarium/Promptarium.csproj --no-restore` が警告・エラーなしで成功 |
| [x] | M2 永続化・ドメイン | SQLiteで資産・保存場所・生成記録・手動編集・手動生成値・タグ・スキャンルートを分離保存。旧カタログへの非破壊列追加もスモークテスト済み |
| [x] | M3 PNG抽出・ComfyUI解析 | PNG `tEXt`／`zTXt`／`iTXt`、prompt／workflow原本保存、標準prompt JSON解析、workflow-only限定導出、状態表示をスモークテスト済み |
| [x] | M4 スキャン・重複処理 | 複数ルートの再帰PNG走査、指定外除外、SHA-256による重複資産統合、移動後の再照合、欠落場所のオフライン化をスモークテスト済み |
| [x] | M5 ライブラリUI | 100件ページングのサムネイルグリッド、更新日時降順、フリーワード／モデル／LoRA／タグ／カテゴリのAND検索を実装。WPF起動継続を確認済み |
| [x] | M6 詳細・編集UI | 情報源表示、手動補足値、評価・メモ・お気に入り、タグ再分類、元／カテゴリ単位コピー、workflow JSON書出し、保存場所表示を実装済み |
| [x] | M7 バックアップ・検証 | SQLiteバックアップ／復元、最小PNG・ComfyUI JSONフィクスチャを用いた実行可能なスモークテスト、警告・エラーなしビルドを確認済み |
| [x] | M8 信頼性・運用性強化 | ローカル診断、差分／完全再解析、中止、フォルダ管理、ComfyUI標準ノード拡張、検索UX、情報源、xUnitを実装。ComboBox、入力、ボタン、チェック、リスト、DataGrid、診断ウィンドウの通常・選択・ホバー・無効状態を高コントラスト配色へ修正し、別出力でのWPF起動・xUnit 4件・スモークテストを確認済み |
| [x] | M9 出力経路限定解析 | `prompt`／`workflow` JSONでSaveImage／PreviewImageなどの出力ノードから入力リンクを逆走査し、未接続のプロンプト・LoRA・モデルを除外。出力経路なしでは値を推測せず一部解析として保持。未接続ノードの除外を含むxUnit 7件、スモークテスト、Release自己完結EXE出力で検証済み |
| [x] | M10 コンディショニング経路解析 | KSamplerの正負入力からReroute／ConditioningConcatを再帰追跡し、接続済みのキャラ・ポーズ・品質だけを入力順に結合。リンクが`null`の未接続入力も安全に無視。代表テストを追加し、提供された4実ワークフローで未接続プロンプト24ノード・LoRA23ノードの混入が0件、xUnit 9件・スモークテスト成功を確認済み |
| [x] | M11 再利用中心の詳細UI | 大きな詳細プレビューを外し、モデル／LoRAを先頭の情報源付きラベルへ変更。ポジティブプロンプトを5分類カテゴリ＋未分類へ入力順で分け、各カテゴリ・全文・ネガティブプロンプトのコピーを実装。カテゴリ変更中もコピー対象を更新し、警告・エラーなしビルド、xUnit 9件、スモークテスト、WPF起動継続で検証済み |

## 直近の作業

- [x] 2026-09-08 保守: プロンプト分割の文字バッファを `StringBuilder` に置換。括弧内のカンマ・不均衡括弧・空要素・Unicodeと内部空白の保持を含むxUnit 14件、スモークテスト、警告・エラーなしビルドが成功。今回の変更ではGUI操作の再検証は未実施。

1. 実ComfyUI PNGが提供された場合、同じ解析結果をPNGメタデータの`prompt` JSON／`workflow` JSONでも確認し、フィクスチャへ追加する。
2. 実データ件数に応じて、10,000件規模の差分スキャン時間を測定する。

## 既知の保留事項

- 実ComfyUI PNGは未提供。最小有効PNGと標準ComfyUI JSONフィクスチャで検証しており、実利用のカスタムノードは未知ノードとして明示表示する。実PNGが得られた場合は対応可否を追加記録する。
- `workflow` JSONのみの解析はComfyUI標準UI構成（ノード、リンク、ウィジェット値）への限定的なベストエフォート導出であり、状態は「一部解析」とする。
- `ウマ娘.json`、`おしまい.json`、`ブルアカ_ノーマル.json`、`魔女っ子.json`の実ワークフローで、Reroute／ConditioningConcat経由の接続済みプロンプト結合と未接続ノードの除外を確認済み。実PNGは未提供のため、PNGに保存された`prompt` JSONとの一致確認は未実施。
- `SaveImage`／`PreviewImage`などの出力ノードがメタデータ内に存在しない場合、未接続ノードを誤って採用しないため、生成値は表示せず「一部解析」とする。
- 10,000件規模の定量的な性能測定は未実施。UIは100件ページング、差分スキャンは同一パス・サイズ・更新日時のファイルでPNG再読込と完全ハッシュを省略し、変更時のみ完全ハッシュで照合する。
- GUI操作を自動駆動するテストは未導入だが、WPF起動継続、本体ビルド、xUnit 9件、データ層の再現可能なスモークテストで検証した。
